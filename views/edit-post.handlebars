
{{> private-nav}}

<main>
  <div class="form-floating">
    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea">{{post.title}}</textarea>
    <label for="floatingTextarea">Title</label>
  </div>
  <img src={{post.image_url}}></img>
  <input placeholder="Add a link to your post image here"> </input>
  <div class="form-floating">
    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea">{{post.body}}</textarea>
    <label for="floatingTextarea">Content </label>
  </div>
</main>